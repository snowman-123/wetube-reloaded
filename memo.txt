npm은 패키지매니저이다
git remote add origin github.url
dependencies : 필요한 패키지. 모듈을 설치해준다
babel nodemon 설치

babel문법사용중
nodejs로 실행안하고 babel-node로 실행중
nodemon : 자동재시작

middleware next()

모듈
import export
default export

라우터, 컨트롤러
/:variable

정규식
ab?cd b는 optinal
ab+cd b는 많아도됨
ab*cd bc사이에 뭐든가능
ab(cd)?e cd=optinal

렌더링은 컨트롤러가 한다.